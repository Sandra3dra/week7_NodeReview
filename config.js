const config = {
    host: "localhost",
    port: 8889, // 8889 for mac, 3306 for win
    uname: "root",
    pword: "root", // leave blank for windows
    database: "db_week7"
}

module.exports = config;